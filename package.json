{
  "name": "sofa-gamr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.166.1"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.9.18"
  },
  "scripts": {
    "build": "vite build",
    "deploy": "gh-pages -d dist"
  }
}
